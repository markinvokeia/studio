
'use client';

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

export default function AlertCategoriesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Alert Categories</CardTitle>
        <CardDescription>Manage alert categories.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Alert categories management will be implemented here.</p>
      </CardContent>
    </Card>
  );
}
