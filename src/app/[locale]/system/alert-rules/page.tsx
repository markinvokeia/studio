
'use client';

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

export default function AlertRulesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Alert Rules</CardTitle>
        <CardDescription>Configure alert rules.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Alert rules configuration will be implemented here.</p>
      </CardContent>
    </Card>
  );
}
