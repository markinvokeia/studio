export const DOCTOR_WORKSPACE_ROLES: string[] = ['medico', 'médico', 'doctor', 'admin', 'administrador'];

/**
 * Roles que identifican a un paciente del portal de auto-servicio (/my-profile).
 * La comparación se hace en minúsculas — ver `usePatientPortal()`.
 */
export const PATIENT_PORTAL_ROLES: string[] = ['paciente', 'patient'];
